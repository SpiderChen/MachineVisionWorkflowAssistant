# 运行环境：Windows Server 2016、Windows 10/11，Python 3.10+（README §2）
# 视觉与输入内核
mss                                  # 快速截屏，多显示器
opencv-python                        # 模板匹配 / 图像处理
numpy
rapidocr-onnxruntime                 # OCR 文字锚点（ONNX，CPU 即可）
# 1.28.0 发布后 Windows PyInstaller 打包失败；1.27.0 是 GitHub Actions 已验证版本。
# 显式固定这个原本由 rapidocr 间接安装的依赖，避免无关发版破坏构建。
onnxruntime==1.27.0
pynput                               # 非 Windows 开发环境的输入降级后端
pywin32; sys_platform == "win32"     # 窗口激活 / 剪贴板
Pillow                               # 托盘图标绘制

# 托盘与配置界面
pystray
PyQt5==5.15.11; sys_platform == "win32"  # Qt5 兼容 Windows Server 2016
PySide6; sys_platform != "win32"         # macOS / Linux 继续使用 Qt6

# 触发源
fastapi
uvicorn
SQLAlchemy>=2.0
pyodbc; sys_platform == "win32"      # SQL Server 主力驱动（其他库装对应驱动即可）
requests                             # 结果回调

# 配置与凭据
PyYAML
keyring                              # Windows 凭据管理器（DPAPI）

# 打包（M5）
# pyinstaller
